#%Module -*- tcl -*-
##
## modulefile
##
proc ModulesHelp { } {

  puts stderr "\tAdds Freesurfer to your environment variables,"
}

module-whatis "adds Freesurfer to your environment variables"

set               root                 /home/inb/soporte/lanirem_software/freesurfer_7.3.2


prepend-path          PATH           $root/bin

setenv                FREESURFER_HOME           $root/
setenv                FREESURFER                $root/
setenv	              FSF_OUTPUT_FORMAT         nii.gz

setenv                SUBJECTS_DIR              $root/subjects
setenv                FUNCTIONALS_DIR           $root/sessions


setenv                FSFAST_HOME               $root/fsfast
setenv                FMRI_ANALYSIS_DIR         $root/fsfast


setenv                MNI_DIR                   $root/mni
setenv                MINC_BIN_DIR              $root/mni/bin
setenv                MINC_LIB_DIR              $root/mni/lib
setenv                MINC_DATAPATH             $root/mni/data
setenv                MNI_PERL5LIB              $root/mni/share/perl5


# podriamos hacer que haga load de fsl como requisito
module load fsl
# prepend-path          FSL_DIR              $root/mni/bin



# del template de lavis pero no exite en nuestra instalacion
# eliminado por lconcha mayo 2025
#prepend-path           LD_LIBRARY_PATH           $root/lib
#prepend-path           LD_LIBRARY_PATH           $root/lib/qt/lib
#prepend-path          DYLD_LIBRARY_PATH         $root/lib/misc/lib

setenv                TESTVAR                   lconcha
